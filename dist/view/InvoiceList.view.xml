<mvc:View xmlns="sap.m" xmlns:mvc="sap.ui.core.mvc" controllerName="UI_FIVE.controller.InvoiceList">
    <Panel accessibleRole="Region">
        <List headerText="{i18n>InvoiceListTitle}" class="sapUiResponsiveMargin" width="auto"
            items="{
    path: 'invoice>/Invoices', sorter: {path: 'ShipperName', group: true} }"
            id="invoiceListId">
            <headerToolbar>
                <Toolbar>
                    <Title text="{i18n>InvoiceListTitle}"></Title>
                    <ToolbarSpacer></ToolbarSpacer>
                    <SearchField width="50%" search=".onFilterInvoices"
                        placeholder="{i18n>ProductNameSearchPlaceHolder}"></SearchField>
                </Toolbar>
            </headerToolbar>
            <items>
                <!-- ExtendedPrice to ExTendedPrice for debuggin' ctrl+shift+alt+s -->
                <!-- Added type and press property after routing  -->
                <ObjectListItem title="{invoice>Quantity} X {invoice>ProductName}"
                    number="{
                parts:[
                    {path:'invoice>ExtendedPrice'}, {path:'view>/currency'}
                ],
                type:'sap.ui.model.type.Currency',
                formatOptions: {
                    showMeasure: false
                }
            }"
                    numberUnit="{view>/currency}"
                    numberState="{= ${invoice>ExtendedPrice}&gt;50 ? 'Error' : 'Success'}"
                    type="Navigation" press="onPressDetails">
                    <firstStatus>
                        <ObjectStatus
                            text="{path:'invoice>Status', formatter:'.formatter.statusText'}">
                        </ObjectStatus>
                    </firstStatus>
                </ObjectListItem>
            </items>
        </List>
        <!-- responsiveness -->
        <!-- Sap m table -->
        <Table headerText="{i18n>InvoiceListTitle}" class="sapUiResponsiveMargin" width="auto"
            items="{
    path: 'invoice>/Invoices', sorter: {path: 'ShipperName', group: true} }"
            id="invoiceListI2">
            <headerToolbar>
                <Toolbar>
                    <Title text="{i18n>InvoiceListTitle}"></Title>
                    <ToolbarSpacer></ToolbarSpacer>
                    <SearchField width="50%" search=".onFilterInvoices"
                        placeholder="{i18n>ProductNameSearchPlaceHolder}"></SearchField>
                </Toolbar>
            </headerToolbar>
            <columns>
                <Column hAlign="End" minScreenWidth="Small" demandPopin="true" width="5em">
                    <Text text="{i18n>columnQuantity}" />
                </Column>
                <Column>
                    <Text text="{i18n>columnName}" />
                </Column>
                <Column minScreenWidth="Small" demandPopin="true">
                    <Text text="{i18n>columnStatus}" />
                </Column>
                <Column minScreenWidth="Tablet" demandPopin="false">
                    <Text text="{i18n>columnSupplier}" />
                </Column>
                <Column hAlign="End">
                    <Text text="{i18n>columnPrice}" />
                </Column>
            </columns>
            <items>
                <ColumnListItem type="Navigation" press="onPressDetails">
                    <cells>
                        <ObjectNumber number="{invoice>Quantity}" emphasized="false" />
                        <ObjectIdentifier title="{invoice>ProductName}" />
                        <Text
                            text="{
								path:'invoice>Status',
								formatter: '.formatter.statusText'
							}" />
                        <Text text="{invoice>ShipperName}" />
                        <ObjectNumber
                            number="{
								parts: [
									'invoice>ExtendedPrice',
									'view>/currency'
								],
								type: 'sap.ui.model.type.Currency',
								formatOptions: {
									showMeasure: false
								}
							}"
                            unit="{view>/currency}"
                            state="{= ${invoice>ExtendedPrice} > 50 ? 'Error' : 'Success' }" />
                    </cells>
                </ColumnListItem>
            </items>
        </Table>
    </Panel>
</mvc:View>